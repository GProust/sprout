# Normalize line endings
* text=auto

# Shell scripts must keep LF (used by the Linux CI runner)
gradlew text eol=lf
*.sh text eol=lf

# Windows scripts keep CRLF
*.bat text eol=crlf

# Treat the Gradle wrapper jar as binary
*.jar binary
